rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 允許所有已登入用戶讀寫所有文檔（測試用）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 